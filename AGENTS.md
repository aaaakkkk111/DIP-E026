# 工作区访问与资源规则

本文件是 Simulation 项目的代理指令，适用于在本项目中执行的任务。

## 文件夹访问边界

- `D:\DIP\Simulation`：允许浏览、检索、引用，并根据用户任务修改其中的内容。
- `D:\DIP\Bluetooth`：允许浏览、检索、引用，并根据用户任务修改其中的所有内容，包括全部子目录和文件。
- `D:\DIP\STM32平衡车_V2`（用户所称的“STM32平衡车文件夹”）：仅供读取、浏览、检索和引用。禁止修改其中任何内容，包括所有子目录和文件。
- 即使工具或运行环境提供了 STM32 平衡车目录的写入权限，也必须将其视为只读目录。禁止在其中新增、覆盖、删除、移动或重命名文件和目录，也不得通过脚本、构建、测试或其他工具间接写入缓存、日志、编译产物或修改配置。
- 如需基于 STM32 平衡车目录中的资料开展开发，应将所需内容复制到 Simulation 或 Bluetooth 目录中，并仅在这两个允许修改的目录内修改或生成结果；原始资料保持不变。

## 缺失资源与访问请求

- 对于允许修改的 Simulation 和 Bluetooth 目录，若执行任务所必需的资源在检查后仍无法找到或访问，应直接向用户提出具体的资源或访问请求。
- 请求时说明缺少什么资源、该资源的用途、已检查的位置，以及获取该资源的具体方式（例如提供文件或实际路径、上传或复制到 Simulation 目录、提供下载来源，或授予所需的读取权限）。无法确定获取来源时应如实说明，不得编造。
- 不得以资源缺失为由绕过访问限制，也不得为了获取资源而修改 STM32 平衡车目录。

## 验证说明

- 2026-09-04：已验证上述两个目录存在、能够列出目录内容，并分别读取 Simulation 的 README.md 和 STM32 平衡车目录中的一个现有文件以确认可读性。
- 2026-09-09：已确认 `D:\DIP\Bluetooth` 目录存在，并根据用户授权将其加入允许修改的目录。
- STM32 平衡车目录的只读要求是本项目的代理行为约束；本文件不会改变操作系统或工具层面的权限，也不会覆盖更高优先级的指令。
